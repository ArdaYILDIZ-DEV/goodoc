# === goodoc — optimal .gitignore ===
# Goal: keep source, templates and content/ clean; exclude generated / cache / OS / IDE artifacts

# --- Build output (generated by build.py) ---
# build/ contains all HTML output — never commit, regenerate with `python build.py`
/build/
# Root index.html is generated by build.py — not tracked (build/ is the deploy artifact).
/index.html

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
ENV/
*.egg-info/
dist/
*.egg
pip-log.txt
pip-delete-this-directory.txt

# Type checkers / linters / test caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.nox/

# --- OS / filesystem ---
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
.Trashes
.Spotlight-V100
directory
*~

# --- Editors / IDE ---
.vscode/
.idea/
*.swp
*.swo
*.swn
*~
.project
.classpath
*.code-workspace
.history/

# --- Logs / temp / backup ---
*.log
*.tmp
*.temp
*.bak
*.orig
*.rej
.temp/
tmp/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Misc ---
# Agent documentation: project-local guidance, intentionally untracked.
AGENTS.md
.env
.env.*
!.env.example
*.pem
*.key

# --- Keep _attachments structure but ignore OS cruft inside ---
# .keep files are intentional to preserve empty _attachments/ folders — DO NOT ignore
!.keep
